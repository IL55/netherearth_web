import * as BABYLON from '@babylonjs/core';

export const attachCameraControls = (scene: BABYLON.Scene, camera: BABYLON.ArcRotateCamera) => {
    // Camera controls removed per user request
    return () => {};
};
