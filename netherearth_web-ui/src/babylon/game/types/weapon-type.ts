/**
 * Specifies the types of weapons that can be equipped on robots.
 * Each weapon type has distinct range and damage characteristics.
 */
export enum WeaponType {
    CANNON = 'cannon',
    MISSILE = 'missile',
    PHASER = 'phaser',
}
