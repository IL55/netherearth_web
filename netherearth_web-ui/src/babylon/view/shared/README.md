# view/shared

Utilities shared across all views. No view-specific logic.

- `models.ts` — loads all GLB assets via `AssetsManager`, returns a name→mesh map
- `scene-utils.ts` — `setVisibleAll()`: recursively shows/hides a node hierarchy
