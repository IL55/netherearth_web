# data

Pure data layer — types, parsing, and loading. No BabylonJS imports.

- `map.ts` — `MapData` interface and `loadMap()` which fetches and parses a `.map` file into tiles and objects
- `robot.ts` — `RobotConfig` interface and the `robotConfigs` array (the 6 robot presets)
